# Решения

Одна строка «почему» обязательна. «Клод так сказал» — не причина: на защите спросят.
Инструмент попадает в «В коде» тем же коммитом, которым появляется его первое настоящее
использование. Пока он в «Предложено» — на него нельзя ссылаться как на существующую
гарантию ни в ревью, ни на защите.

## В коде — выбрано не обсуждением, а тем, что уже написано

В колонке «маркер» — то, что можно проверить `grep`'ом, а не путь, который может
ещё не приехать из ветки. Маркера нет в рабочем дереве — значит эта часть кода
пока в ветке, а не здесь; сам выбор от этого не меняется.

| Что | Чем | Маркер |
|---|---|---|
| HTTP | Gin | `gin-gonic/gin` в `backend/go.mod` |
| Драйвер БД | pgx (без ORM, без sqlc) | `jackc/pgx` в `backend/go.mod` |
| Миграции | goose | `-- +goose Up` в `backend/migrations/*.sql` |
| WebSocket | gorilla (решено 06.08, см. ниже) | `gorilla/websocket` в `backend/go.mod` |
| Линтер | golangci-lint v2, конфиг в корне | `.golangci.yaml` |
| Мок для фронта | Prism 5.16.0 из `openapi.json`, с валидацией запросов | цель `mock` в `Makefile` |
| Кодоген Go из контракта | oapi-codegen v2.8.0, **только типы** | `backend/internal/api/types.gen.go` |
| Кодоген TS из контракта | openapi-typescript (выбрал фронт, версия — в самом скрипте) | скрипт `gen:api` в `frontend/package.json` |
| UI-библиотека | Chakra UI **v3** (выбрал фронт) | `"@chakra-ui/react": "^3"` в `frontend/package.json` |
| Go | `1.25.0` | строка `go` в `backend/go.mod` |
| Общая инфраструктура | `backend/pkg/`, не `internal/platform/` | каталог `backend/pkg/` |

## Решено командой

| Дата | Что | Решение | Почему |
|---|---|---|---|
| 06.08 | Модель авторизации | **`login` + `password`** | Контракт (`nickname`) описывал заглушку; `feat/auth` уже построен на пароле. **Следствие: `docs/openapi.json` надо привести к решению — это блокирует всю работу по `/auth/*`** |
| 06.08 | Энергия/прогресс | **Уход (`feed`/`play`/`wash`) начисляет XP и засчитывает день стрика** | Так уже сделано в `feat/pet-service`, и контракт это допускает (`dailyCareXpCap`). Если решим убрать — выставим XP за уход в 0, ломать логику не придётся |
| 06.08 | `go.mod` | Переехать в `backend/`, **первым коммитом реконсиляции** | Корень модуля не совпадает с корнем кода — это уже дважды ломало Stop-хук и заставляет каждый скрипт помнить про `cd backend` (в Makefile и в CI). Проверено мержем: переезд не даёт конфликта у тех, кто останется на старой раскладке (git видит rename, чужие правки `go.mod` переносятся сами). Единственный побочный эффект — новые файлы с импортом `tamagochi/backend/...` перестанут компилироваться, чинится одним `goimports` |
| 06.08 | Гонка на claim | Тест против настоящего Postgres по `TEST_DATABASE_URL`, `t.Skip` если пусто | Мок не доказывает однократность выдачи. `docker compose` уже есть — это дешевле testcontainers и работает сегодня |
| 06.08 | Тесты со случайными данными | Дифференциальный тест против наивной модели, seeded `math/rand` | Распад состояния считается «лениво» (в закрытой форме от `LastDecayAppliedAt`), наивная модель — по шагам. Расхождение = баг. Фаззинг парсеров (`encoding/json` + сгенерированные типы) такого класса багов не ловит |
| 06.08 | `REWARD_NOT_ELIGIBLE` | 403 → **409** | Это состояние, а не права доступа |
| 06.08 | Layout (целевой) | `handler → service → repo`, пакет на тег OpenAPI | Границы кода = границы контракта, `docs/ARCHITECTURE.md` |
| 06.08 | Время в домене | параметром, не `time.Now()` внутри | Иначе decay/стрик нечем тестировать, и демо-сдвиг часов станет дорогим |
| 06.08 | WebSocket | **gorilla**, не coder/websocket | Уже написан в `feat/pet-service`, переписывать под дедлайн дороже. Цена: gorilla требует, чтобы в соединение писала одна горутина — менеджеру нужен мьютекс на запись до появления пуша состояния. На фронте состояние из WS вливать в кэш RTK Query (`onCacheEntryAdded`), иначе в интерфейсе будет два разных питомца |

## Предложено — можно оспорить, владельца пока нет

Кандидаты, а не решения. Пункт уезжает отсюда в «В коде» тем же коммитом,
которым появляется его первое настоящее использование — если он всё ещё здесь,
значит в коде его нет.

- **`testing/synctest`** — стандартная библиотека Go 1.25, ставить нечего, но пока
  не используется ни в одном тесте. Для того, где время
  параметром не сделать (WS-хартбиты, таймауты реконнекта).

- **Серверные интерфейсы oapi-codegen** (генератор `gin-server`) — сегодня генерируются
  только типы. `gin-server` выдаёт `ServerInterface` сразу на все операции контракта,
  и его подключение уронит сборку у всех, кто реализовал не все, — то есть у обеих
  веток фич в момент мержа. Возвращаться к этому после реконсиляции, пакет за пакетом.
- **kin-openapi** — middleware валидации запроса/ответа. Второй слой поверх кодогена,
  ловит то, что компилируется, но нарушает enum/required/format.
- **testcontainers-go** — герметичный Postgres в тестах; нужен, только если
  `TEST_DATABASE_URL` окажется неудобным в CI.
- **Playwright** — один сквозной сценарий.

## Померено, но не взято

**`go tool` для golangci-lint** (директива `tool` в `go.mod`, Go 1.24+). Решило бы
расхождение версий линтера между машинами по-настоящему: версия пинится в
репозитории, ставить глобально ничего не надо, и — проверено — оно работает там,
где `go run golangci-lint@версия` падает, потому что `go tool` собирает линтер
тулчейном модуля, а не базовым.

Не берём сейчас, и вот цена, она измерена: `go get -tool golangci-lint@v2.5.0`
раздувает `go.mod` с 9 строк до 220, а `go.sum` — с 2 до 947. Весь граф
зависимостей линтера переезжает в модуль проекта. `go.mod` и так единственный
файл, который конфликтует с обеими ветками фич, — раздувать его вдвадцатеро
именно сейчас значит сделать мерж заметно хуже ради проблемы, которая уже
обойдена: `pre-push` по линтеру не блокирует, а источник истины — CI.

Вернуться к этому имеет смысл после реконсиляции, когда `go.mod` перестанет быть
горячей точкой.

Для сравнения — цена уже взятого кодогена, тоже измерена. Сам oapi-codegen в модуль
не попадает вовсе: версия запинена в `//go:generate` (`go run ...@v2.8.0`), и здесь
этот способ работает, потому что кодоген читает JSON-спеку, а не исходники модуля —
то, на чём падал линтер. В `go.mod` приезжает только рантайм сгенерированных типов,
`oapi-codegen/runtime`: `go.mod` 9 → 12 строк, `go.sum` 2 → 12. Он даёт типизацию
там, где спека говорит `format: uuid` и `format: date`, вместо голых строк.

## Отложено — вернуться, если будет время

sqlc (pgx хватает, а вторая кодогенерация на третий день — риск) · rapid
(property-тесты; нужен только когда доменные функции уже есть, даёт shrinking
падающего кейса) · santhosh-tekuri/jsonschema (валидация правил-как-данных: нужен, только если
правила станут данными в JSONB) · `go test -fuzz` парсеров · CI-джобы для фронта и e2e (нет `frontend/`).

## Открытое — остановись и спроси

**Entitlement наград.** `cosmetic` выдаётся напрямую; `soft`/`premium` всё ещё несут
буквальный `promoCode`, который можно переслать. Разбор предлагал entitlement. Прямо
касается главного правила кейса про анти-абьюз — самое дорогое из открытого.

**Кривая уровней.** 25 уровней / `100×1.25^(L-1)` против 12 почти линейных + сезоны.
Трогает экраны фронта и баланс наград.

**Концепт питомца.** Не выбран.

**Redis.** По умолчанию нет: Postgres тянет лидерборд и фанаут на масштабе хакатона,
а второй сервис с состоянием под дедлайн — это новая инфраструктура и лишний вопрос на защите.
Хочешь Redis — приходи с обоснованием, это не дефолт.

## Не решение, а несделанная работа

- Деплой на публичный URL
- `make seed` и служебный сдвиг часов (`APP_ENV=demo`) — жюри смотрит десять минут и не
  видит, как проходит время; сдвиг дёшев ровно потому, что время приходит параметром
- README для защиты: ограничения MVP, ключевые решения (день 6, не сейчас)
